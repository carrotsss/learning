package alg.binaryOper;

/**
 * 第一个错误版本
 * 给定一个元素n代表有[1,2,3,...n]版本，再第x个位置开始出现错误版本，导致后面的版本都错误。可以调用isbadversion可以知道是哪个版本错误，找到第一个错误版本
 */
public class P4_278_FirstBadVersion {
//    public static int findFirstBadVersion(int n) {
//        int l = 1, h = n;
//        while (l < h) {
//            int mid = l + (h - 1) / 2;
//            if (isBadVersion(mid)) {
//                h = mid;
//            } else {
//                l = mid + 1;
//            }
//        }
//        return l;
//    }
}
