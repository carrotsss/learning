package datastructure.list;

/**
 * 设计一个链表:
 * 在链表类中实现这些功能：
 * 1、get(index):获取链表中第index个节点的值。如果索引无效，则返回-1
 * 2、addAtHead(val):在链表的第一个元素之前添加一个值为val的节点。插入后，新节点将成为链表的第一个节点
 * 3、addAtTail
 * 4、addAtIndex(index,val)
 * 5、deleteAtIndex(index)
 */
public class P2_707_DesignList {
}
